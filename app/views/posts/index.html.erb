<%= link_to "新規投稿する", "/posts/new" %>

<% @posts.each do |post| %>
  <p>
    <%= post.name %>
    <%= post.content %>
    <%= link_to "編集", "/posts/#{post.id}/edit" %>
    <%= link_to "削除", "/posts/#{post.id}", method: :delete %>
  </p>
<% end %>
